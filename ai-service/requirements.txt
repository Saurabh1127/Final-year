# ── Core FastAPI & Server ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1
python-multipart==0.0.9
websockets==12.0

# ── Utilities ────────────────────────────────────────────────────────────────
numpy<2
scipy

# ── ASR: OpenAI Whisper (small model for production accuracy) ─────────────────
openai-whisper==20240930
torch
torchaudio

# ── NMT: Meta NLLB-200 (Hugging Face Transformers) ────────────────────────────
transformers==4.44.2
sentencepiece==0.2.0
protobuf==5.27.2
accelerate

# ── TTS: gTTS (primary, zero-latency cloud TTS across 100+ languages) ─────────
gTTS==2.5.1

# ── TTS: Coqui XTTS-v2 (voice retention skeleton - install separately on GPU) ──
# TTS>=0.22.0   # Uncomment when running on Colab T4 / RTX 3050 with CUDA
